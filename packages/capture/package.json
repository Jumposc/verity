{
  "name": "@solvir/verity-capture",
  "version": "0.0.0",
  "description": "Verity capture: in-browser DOM style extraction (injected by Playwright / Chrome DevTools MCP).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./inject": "./dist/inject.global.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean && tsup src/inject.ts --format iife --global-name __verityCapture --no-clean",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@solvir/verity-core": "workspace:*"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
